<Window x:Class="WpfUP_GM.Windows.AddChangeBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfUP_GM.Windows"
        mc:Ignorable="d"
        Title="AddChangeBookWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название" VerticalAlignment="Center" Margin="5"/>
        <TextBox Name="BookName" Grid.Row="0" Grid.Column="1" Margin="5" TextWrapping="Wrap" 
             Text="{Binding Name}" Height="30"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Содержимое" VerticalAlignment="Center" Margin="5"/>
        <TextBox Name="BookText" Grid.Row="1" Grid.Column="1" Margin="5" TextWrapping="Wrap" 
             Text="{Binding Text}" Height="80"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Обложка" VerticalAlignment="Center" Margin="5"/>
        <TextBox Name="BookCover" Grid.Row="2" Grid.Column="1" Margin="5" TextWrapping="Wrap" 
             Text="{Binding Cover}" Height="30"/>
        <StackPanel Grid.Row="3" Grid.RowSpan="2" Margin="5" VerticalAlignment="Center">
            <ComboBox x:Name="GenreComboBox" Width="160" Height="40" IsEditable="True" 
                  DisplayMemberPath="GenreName" SelectedValuePath="Id" Margin="5"/>
            <Button Name="AddGenre" Height="Auto" Content="Добавить" Click="AddGenre_Click"/>
        </StackPanel>
        <ListBox x:Name="GenresList" Grid.Column="1" Grid.Row="3" Grid.RowSpan="2"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding Genre.GenreName, StringFormat=Жанр: {0}}"/>
                        <Button Content="Удалить" Height="30" Click="DeleteGenre_Click"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Сохранить" Width="80" Margin="5" Click="ConfirmButton_Click"/>
            <Button Content="Отмена" Width="80" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
