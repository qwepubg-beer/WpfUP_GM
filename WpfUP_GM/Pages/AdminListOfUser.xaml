<Page x:Class="WpfUP_GM.Pages.AdminListOfUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfUP_GM.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminListOfUser">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Grid.Column="1" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Name="Save" Width="Auto" Content="Сохранить" Click="Save_Click" Grid.Column="0"/>
            <Button Name="Add" Width="Auto" Content="Добавить" Click="Add_Click" Grid.Column="1"/>
        </Grid>
        <DataGrid x:Name="UserDataGrid" AutoGenerateColumns="False" Grid.ColumnSpan="2" Grid.Row="1" FontSize="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Binding="{Binding login}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding password}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Номер роли" Binding="{Binding Role}"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role.Name}" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
